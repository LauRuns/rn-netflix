import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const ExpNFItem = (props) => {
	return (
		<View style={styles.screen}>
			<Text>Exp item works</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	screen: {}
});
